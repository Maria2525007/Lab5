package commands;

import utility.CollectionManager;

import java.util.Queue;

public class RemoveAllByLocation {
    public RemoveAllByLocation(CollectionManager collectionManager, Queue<String> previousCommands) {
    }
}
