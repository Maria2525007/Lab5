package commands;

import utility.CollectionManager;
import utility.StudyGroupFactory;

import java.util.Queue;

public class RemoveLower {
    public RemoveLower(StudyGroupFactory studyGroupFactory, CollectionManager collectionManager, Queue<String> previousCommands) {
    }
}
